# AUTOPS 全面修复总结

> **项目**：https://github.com/wusdic/autops-zhipu
> **修复日期**：2026-06-25
> **范围**：安全、正确性、性能、错误处理、代码质量全覆盖
> **规模**：6 个批次 / 56 个文件 / +3023 −1454 行

## 修复概览

| 批次 | 主题 | 严重程度 | 提交 |
|------|------|---------|------|
| 批1 | 阻断性 Bug（功能崩溃/数据泄漏） | 严重 | `41c09bc` |
| 批2 | RBAC 鉴权层（零迁移） | 严重/高 | `a8e7334` |
| 批3 | 注入与安全加固 | 严重/高 | `7f1d0e0` |
| 批4 | 事件一致性与并发 | 高 | `bdc29a8` |
| 批5 | 正确性与业务逻辑 | 中 | `6953a43` |
| 批6 | 代码质量与一致性 | 中/低 | `1137ef6` |

---

## 第 1 批：阻断性 Bug

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 1.1 | CRUDService 引用不存在的 `model_class`，调用即崩 | `common/crud_service.py` | 改用 `self.repo.model`；增强 `model_to_dict` 类型序列化 |
| 1.2 | WebSocket 匿名连接 + 广播反转 + 未知事件泄漏 | `api/websocket.py` | 无 token 拒绝；未订阅不收；锁外发送 |
| 1.3 | `anext(get_db())` 连接泄漏 + `/ready` 回显 `str(e)` | `api/health.py` | `async_session_factory` 正确关闭；仅返回 ok/error |
| 1.4 | `get_redis()` 漏 await（2 处） | `api/dashboard.py` `api/platform_extra.py` | 补 await |
| 1.5 | CertCollector 误用私有 API 功能失效 | `workers/builtin_collectors.py` | 改用 `getpeercert()` |
| 1.6 | ping 写死 `-c/-W` Windows 不兼容 + IP 未校验 | `workers/builtin_collectors.py` | 按平台分支 + `ipaddress` 校验 |
| - | DBCollector 同步 recv 阻塞事件循环 | `workers/builtin_collectors.py` | 改异步 `reader.read` |

## 第 2 批：RBAC 鉴权层

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 2.1 | 全平台缺授权层，任意用户可删租户/审批/读全量审计 | `common/auth_dependency.py` | 新增 `require_admin` 依赖（复用现有 Role 表，零迁移） |
| 2.2 | `UserResponse` 不返回 roles，前端 v-permission 失效 | `governance/schemas.py` `service.py` | 加 roles 字段 + `selectinload` 预加载（修 async lazy-load） |
| 2.3 | 高危端点无授权 | 9 个 api.py | create/delete user、script、playbook、asset、approve/rollback、审计/备份/租户挂 require_admin |
| 2.4 | `/my-pending` IDOR（user_id 来自 Query） | `api/dashboard.py` | 改从 `request.state.user_id` 取 |
| 2.5 | WS header 绕过认证 | `common/auth_middleware.py` | 改依据 ASGI scope type；异常仅 catch UnauthorizedError |

## 第 3 批：注入与安全加固

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 3.1 | 命令策略相对路径绕过 | `automation/command_policy.py` | 路径校验扩展到所有非选项 token |
| 3.2 | AI Agent 工具 f-string 拼 asset_ids + guard 忽略 args | `aiops/tools/execution.py` `guard.py` | `json.dumps` + ID 校验 + args 内容扫描 |
| 3.3 | SQL paginate f-string 拼接 | `infra/db_dialect.py` | 强制 int + 范围校验；未知方言快速失败 |
| 3.4 | 凭证加密复用 JWT 密钥 + 硬编码盐 | `common/crypto.py` `infra/config.py` | 独立 `CREDENTIAL_ENCRYPT_KEY` + 每条随机盐 |
| 3.5 | 自研 HTML 净化器可绕过 XSS | `frontend sanitize.ts` | 改用 DOMPurify |
| 3.6 | 知识库 v-html 未净化 + 搜索高亮 XSS | `KnowledgeEditPage.vue` `GlobalSearchOverlay.vue` | 套 sanitizeHtml / 先 HTML 转义 |

## 第 4 批：事件一致性与并发

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 4.1 | outbox 写入独立 session commit，与业务事务非原子 | `common/events.py` `workers/scheduler.py` | `publish` 接受 session 参数复用业务事务 |
| 4.2 | `run_execution` 同步阻塞事件总线 300s | `aiops/handlers.py` | 改 `asyncio.create_task` 后台执行 |
| 4.3 | 幂等去重 `clear()` 全量清空 | `aiops/alert/ticket handlers.py` | 改 OrderedDict LRU 淘汰 |
| 4.4 | 后台 task 句柄丢失 + `except: pass` | `asset/discovery_service.py` | 句柄存模块级 set；start_task 显式 commit；except 记日志 |
| 4.5 | 发布版本并发竞态产生多个 published | `config/service.py` | 加 `with_for_update` 行锁 |

## 第 5 批：正确性与业务逻辑

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 5.1 | list_scripts/list_executions count 漏 filter | `automation/service.py` | count 复用相同 where |
| 5.2 | 审批状态过滤恒空 + approve 无状态校验 | `api/automation_extra.py` | effective_status；仅 pending_approval 可审批 |
| 5.3 | `convert_to_ticket` 裸 except 吞所有异常 | `anomaly/service.py` | 区分 ImportError 与业务异常 |
| 5.4 | import_assets 静默跳过，丢失失败明细 | `asset/service.py` `api.py` | 返回 `{created,skipped,errors}` |
| 5.5 | escalate 无限 append + meta 类型未校验 | `anomaly/service.py` | 最高级跳过；`isinstance` 校验 |

## 第 6 批：代码质量与一致性

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 6.1 | `datetime.utcnow()` 全项目 31 处（naive，已弃用） | 14 个文件 | 统一 `datetime.now(timezone.utc)` |
| 6.2 | 通用异常处理器不记日志 | `common/exceptions.py` | `logger.exception` + trace_id |
| 6.3 | 多处 `except: pass` 静默吞异常 | `aiops/service.py` 等 | 改 `logger.debug` |
| 6.4 | Redis/后端端口对外暴露；硬编码密钥；默认弱口令 | `docker-compose.yml` `configs/*.yaml` `init_data.py` | 端口绑回环；密钥走环境变量；随机初始口令 |
| 6.5 | 前端 fetchUser 未启用；WS 幽灵重连；默认凭据；lang/title | 4 个前端文件 | 全部修正 |

---

## 验证

- **后端**：所有改动文件通过 `py_compile` 编译；通过 `ruff check`（无新增 F 类错误，且总体 lint 错误数下降）；crypto 加密/解密 round-trip 测试通过（随机盐生效）；db_dialect 校验测试通过
- **遵循约束**：ruff line-length 88、import 排序、mypy strict 兼容

## 未处理（按计划边界，留待后续）

以下属架构级演进或需更大决策，本轮明确未动：
- outbox 改为消息队列（Kafka/RabbitMQ）
- 幂等去重迁移到 Redis SETNX（多 worker 场景）
- RBAC 细化为 domain:action 权限码矩阵（本轮用粗粒度 admin）
- token 存储改 HttpOnly cookie（本轮保留 localStorage，已通过修 XSS 缩小暴露面）
- 数据库迁移 schema 治理（event_outbox 三处定义冲突等）

## 提交记录

```
1137ef6 fix(批6): 代码质量与一致性
6953a43 fix(批5): 正确性与业务逻辑
bdc29a8 fix(批4): 事件一致性与并发
7f1d0e0 fix(批3): 注入与安全加固
a8e7334 fix(批2): RBAC鉴权层
41c09bc fix(批1): 阻断性Bug修复
```

全部已推送至 `main` 分支。
