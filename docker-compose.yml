version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: autops-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-autops_root_2026}
      MYSQL_DATABASE: autops
      MYSQL_USER: autops
      MYSQL_PASSWORD: ${DB_PASS:-autops_2026}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./deploy/docker/mysql/init.sql:/docker-entrypoint-initb.d/init.sql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: autops-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: autops-backend
    restart: always
    ports:
      - "${BACKEND_PORT:-8001}:8001"
    environment:
      AUTOPS_ENV: prod
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USER: autops
      DB_PASS: ${DB_PASS:-autops_2026}
      DB_DATABASE: autops
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
      LLM_BASE_URL: ${LLM_BASE_URL:-http://host.docker.internal:8000/v1}
      LLM_MODEL_NAME: ${LLM_MODEL_NAME:-qwen3.5-0.8b}
      AUTOPS_ENABLE_SCHEDULER: "false"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./configs:/app/configs:ro

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: autops-worker
    restart: always
    entrypoint: ["python", "-m", "app.workers.runner"]
    environment:
      AUTOPS_ENV: prod
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USER: autops
      DB_PASS: ${DB_PASS:-autops_2026}
      DB_DATABASE: autops
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
      LLM_BASE_URL: ${LLM_BASE_URL:-http://host.docker.internal:8000/v1}
      LLM_MODEL_NAME: ${LLM_MODEL_NAME:-qwen3.5-0.8b}
      AUTOPS_ENABLE_SCHEDULER: "true"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./configs:/app/configs:ro

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: autops-frontend
    restart: always
    ports:
      - "${FRONTEND_PORT:-80}:80"
    depends_on:
      - backend

volumes:
  mysql_data:
  redis_data:
