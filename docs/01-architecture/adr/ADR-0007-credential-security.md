# ADR-0007 凭证安全方案

## 状态
accepted

## 背景
平台需要存储和管理各类凭证（SSH 密钥、数据库密码等）。

## 决策
Fernet 对称加密，主密钥从环境变量加载。

## 备选方案
1. Vault — 额外依赖
2. 明文存储 — 不安全
3. 数据库加密函数 — 数据库绑定

## 影响
- 主密钥不写入代码
- AI 不读取明文凭证
- 凭证使用有审计记录
