# ADR-0003 LLM 集成方式

## 状态
accepted

## 背景
AIops 模块需要调用大语言模型进行故障分析。本地有 vLLM 服务。

## 决策
通过 OpenAI 兼容 API 协议调用 LLM，模型名称和地址从配置文件加载。

## 备选方案
1. LangChain — 过度抽象
2. 直接集成特定 SDK — 绑定供应商
3. 本地模型加载 — 资源消耗大

## 影响
- 模型不可用时平台优雅降级
- 模型名称、地址、温度等参数全部配置化
- AI 输出必须经过策略校验，不允许直接执行
