# ADR-0002 缓存和消息队列

## 状态
accepted

## 背景
平台需要缓存（状态缓存、会话缓存）和消息队列（事件传递、异步任务）。

## 决策
Redis 同时承担缓存和轻量消息队列职责。

## 备选方案
1. RabbitMQ/Kafka — 过于复杂
2. Memcached — 无持久化
3. 内存缓存 — 不支持多进程

## 影响
- Redis 通过封装层访问，便于替换
- 不使用 Redis 的高级特性（如 Lua 脚本）
- 消息队列只用于内部事件，不对外暴露
