# AUTOPS 环境变量配置

# 数据库
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=autops
DB_PASSWORD=autops_2026
DB_DATABASE=autops

# Redis
REDIS_URL=redis://127.0.0.1:6379/0

# LLM 配置
LLM_BASE_URL=http://127.0.0.1:8000/v1
LLM_MODEL=qwen3.5-0.8b

# 安全
JWT_SECRET=change-me-to-random-secret-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480

# 凭证加密密钥 (python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
CREDENTIAL_ENCRYPT_KEY=change-me-to-real-fernet-key

# 后端
BACKEND_PORT=8001
